from setuptools import setup, find_packages

setup(
    name='ns_text_extraction',
    version='0.0.1',
    description='A package for extraction of text features using NLP/LLMs from NeuroStore articles',
    author='Psychoinformatics Lab',
    author_email='delavega@utexas.edu',
    packages=find_packages(),
    install_requires=[
        'openai',
        'pandas',
        'scispacy',
        'pubget'
    ]
)